bun prettier $(git diff --cached --name-only --diff-filter=ACMR | sed 's| |\\ |g') --write --ignore-unknown
git update-index --again
if git diff --cached --name-only | grep -q '^packages/contracts/'; then cd packages/contracts; bun tests; fi
